{
  "name": "1216",
  "version": "1.0.0",
  "private": true,
  "description": "Private web messaging for two",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:server": "npm start --prefix server",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web"
  }
}
